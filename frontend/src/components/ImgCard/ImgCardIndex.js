import React from 'react';
import styled from 'styled-components';
import tw from 'twin.macro';

const ImgCardIndex = (img, title, author) => {

};

const CardContainer = styled.div`

`
export default ImgCardIndex;